#pragma once
#include <iostream>
#include <vector>
using namespace std;

vector<string> Split(const string &, char);
bool isInteger(const string &);
vector<int> convertArrayToInt(vector<string> &array);